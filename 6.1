#include <stdio.h> 
#include <math.h>
int main() 
{ 
	int luku, i;
	float juuri;
	i = 1;
	printf("Anna kokonaisluku:");
	scanf("%d", &luku);

	printf("\nLuku\tNeliöjuuri\n\n");
	
	
	for(i = 1; luku <= luku; ++i) { 

		juuri = sqrt(i);
		printf("\n%d\t%.3f\n", i, juuri);
		
		if(luku == i) 
       { 
         break; 
       }
	}
	
	return 0; 
}
