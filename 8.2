#include <stdio.h> 
     
int main() 
{ 
int matriisi[5][5] =
{
4, 6, 25, 88, 5,
34, 5, 300, 4, 65,
78, 43, 11, 90, 125,
98, 585, 12, 63, 21,
45, 35, 9, 5, 1
}; 

	int summa_matriisi = 0; 
	int y, x; 
	
	printf("Taulukon:\n");
	
	for (y=0; y<5; y++)
	{
		for (x=0; x<5; x++)
		{
			printf("%d ",matriisi [y][x]);	
		}
		if (x==5)
		{
			printf("\n");
		}
	}
	printf("\n\n");
for(y = 0; y < 5; y++) 
{ 
		for(x = 0; x < 5; x++) 
		{ 
			summa_matriisi = summa_matriisi + matriisi[y][x];
		}
			if(y == 4 ) 
			{ 
				printf("alkioiden summa on %d\n", summa_matriisi); 
			}
	}
	return 0;
}
 
