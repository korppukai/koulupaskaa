#include <stdio.h> 
 
 
int tarkista_pienin	(int a, int b, int c);
int tarkista_suurin	(int a, int b, int c);

int eka_luku;
int toka_luku;
int kolmas_luku;

int main()     
{
int suurin;
int pienin;
	
	printf("Anna 1. luku:");
	scanf("%d", &eka_luku);
	
	printf("Anna 2. luku:");
	scanf("%d", &toka_luku);
	
	printf("Anna 3. luku:");
	scanf("%d", &kolmas_luku);
	
	pienin = tarkista_pienin (eka_luku, toka_luku, kolmas_luku);
	suurin = tarkista_suurin (eka_luku, toka_luku, kolmas_luku);
	
	printf("Syöttämistäsi luvuista suurin oli %d ja pienin %d.", suurin, pienin);
	return 0;
}  


int tarkista_pienin(int eka_luku, int tokaluku, int  kolmas_luku) 
{ 
	int pienin;

	if (eka_luku < toka_luku)
	{						
		if (eka_luku < kolmas_luku)
		{
		pienin = eka_luku;
		}
	}
	if (toka_luku < kolmas_luku)
	{
		if (toka_luku < eka_luku)
		{
		pienin = toka_luku;
		}
	}
	if (kolmas_luku < eka_luku)
	{
		if (kolmas_luku < toka_luku)
		{
		pienin = kolmas_luku;

		}
	}
	return pienin;
}
int tarkista_suurin(int eka_luku,int tokaluku,int kolmas_luku)  
{
	int suurin;
	
	if (eka_luku > toka_luku)
	{						
		if (eka_luku > kolmas_luku)
		{
		suurin = eka_luku;
		}
	}
	if (toka_luku > kolmas_luku)
	{
		if (toka_luku > eka_luku)
		{
		suurin = toka_luku;
		}
	}
	if (kolmas_luku > eka_luku)
	{
		if (kolmas_luku > toka_luku)
		{
		suurin = kolmas_luku;

		}
	}
	return suurin;
}
